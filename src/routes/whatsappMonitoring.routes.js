const router=require('express').Router();const auth=require('../middlewares/auth.middleware');const roles=require('../middlewares/role.middleware');const c=require('../controllers/whatsappMonitoring.controller');router.use(auth,roles('admin'));router.get('/status',c.status);router.get('/metrics',c.metrics);router.get('/incidents',c.listIncidents);router.get('/incidents/:id',c.detail);router.patch('/incidents/:id/review',c.review);router.get('/retries',c.retries);router.post('/retries/:type/:id/retry',c.retry);router.post('/retries/:type/:id/cancel',c.cancel);router.get('/jobs',c.jobs);router.post('/check-meta',c.checkMeta);router.get('/export',c.exportReport);module.exports=router;
